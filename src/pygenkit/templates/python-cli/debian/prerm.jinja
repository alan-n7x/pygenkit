#!/bin/sh

set -e

case "$1" in
    remove|upgrade|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            systemctl stop {{ module_name }} >/dev/null 2>&1 || true
            systemctl disable {{ module_name }} >/dev/null 2>&1 || true
        fi
        ;;
esac

exit 0
