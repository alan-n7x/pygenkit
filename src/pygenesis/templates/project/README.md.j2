# {{ project.name }}

{{ project.description }}

## Installation

```bash
pip install {{ project.name }}
```

Or via uv:

```bash
uv tool install {{ project.name }}
```

## Usage

```bash
{{ module_name }} --help
```
